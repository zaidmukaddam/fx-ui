diff --git a/dist/components/floating.js b/dist/components/floating.js
index b04b3f0d0e7ac5aa498075737879ff890ecd159e..11f89eaa92aac08a48af7b94a63d43df7e0996c1 100644
--- a/dist/components/floating.js
+++ b/dist/components/floating.js
@@ -108,6 +108,13 @@ export const FloatingLayer = forwardRef(function FloatingLayer({ side = "bottom"
     const offset = side === "top" || side === "bottom"
         ? { x: alignOffset, y: 0 }
         : { x: 0, y: alignOffset };
-    return (_jsx("anchored", { side: side, align: align, gap: sideOffset, offset: offset, fit: "snap", snapMargin: collisionPadding, deferred: true, priority: 1, occlude: true, children: _jsx("div", { ...props, ref: ref, style: mergeStyles({ backgroundColor: "#1A1A1A" }, props.style), children: children }) }));
+    // `anchored` paints its own opaque surface behind the content, and it is
+    // square. Rounded content therefore shows that surface poking out at each
+    // corner, so give the surface the same radius the content asked for.
+    const contentStyle = typeof props.style === "object" ? props.style : null;
+    const anchoredStyle = contentStyle?.borderRadius === undefined
+        ? undefined
+        : { borderRadius: contentStyle.borderRadius };
+    return (_jsx("anchored", { side: side, align: align, gap: sideOffset, offset: offset, fit: "snap", snapMargin: collisionPadding, deferred: true, priority: 1, occlude: true, style: anchoredStyle, children: _jsx("div", { ...props, ref: ref, style: mergeStyles({ backgroundColor: "#1A1A1A" }, props.style), children: children }) }));
 });
 //# sourceMappingURL=floating.js.map
\ No newline at end of file
